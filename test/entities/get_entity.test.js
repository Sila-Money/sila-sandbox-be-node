// third party packages
const { expect } = require('chai');

describe.only('tests Sila API integration', () => {
    it('shows britt how to use git!', async () => {

        expect(1).to.equal(1);
    });
});